//import generator from 'generate-password'

export default () => {
  /*
  REGLA
  const password = generator.generate({

    length: 10,
    numbers: true,
    uppercase: true,
    lowercase: true,
    symbols: false,
    strict: true
  })
   */

    return "N123asc!!"
}

